#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged on TypeScript files
pnpm lint-staged

# Type check (skip library check for node_modules)
echo "🔍 Running TypeScript type check..."
pnpm tsc --noEmit --skipLibCheck || exit 1

echo "✅ Pre-commit checks passed!"
